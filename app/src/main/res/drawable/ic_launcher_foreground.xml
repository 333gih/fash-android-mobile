<?xml version="1.0" encoding="utf-8"?>
<!-- Foreground layer for the adaptive icon (API 26+).
     References the density-specific fash_icon_fg.png files (no background).
     The canvas is 108dp; the safe zone (always visible under any mask) is the
     inner 72dp × 72dp centred area. -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/fash_icon_fg"
    android:gravity="center"
    android:tileMode="disabled" />
